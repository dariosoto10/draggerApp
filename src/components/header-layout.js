import React from 'react';

function Header() {
  return (
    <header className="App App-header">
      <h1 className="App-title">Welcome to Dragger</h1>
      <p className="App-intro">Start by dragging the small board inside the big board and see the current position on the right</p>
    </header>
  )
}

export default Header;
